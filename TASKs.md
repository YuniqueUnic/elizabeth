当前项目的任务：
当前还有功能未能实现或者存在问题，请你修复并完成当前存在的问题：

1. 顶栏的复制和下载导出这两个按钮是对应中间的消息的，中间的消息应该支持选中/多选/全选/取消选中/反转选中，然后复制则是将用户选中的
   messages 合并并且复制，下载导出则是下载为 markdown。（而是否包含 message 的
   metadata 呢？这个是可以通过 settings 中进行配置的，messages 的 metadata
   应该包含时间，number）
   1. [x] 前端设计已经完成
   2. [ ] 实际业务逻辑实现
2. [x] 文件管理也应加上全选/取消选择/反转选择这些功能按钮
3. 中间的 markdown 相关的内容过于简陋了。我希望能够调用成熟的 markdown
   编辑器替代现在的底部编辑器，并且点击展开编辑器之后，也能够调用完整的成熟的
   markdown 编辑器，支持侧边预览/实时预览等等功能。，(成熟的 markdown 编辑器 lib
   需要你去调研和找出合适的)
   1. [ ] 上网查询/调研合适的 markdown 编辑器
   2. [ ] 替换当前的实现
   3. [ ] ...
4. 当然中间那一栏的 messages 逻辑还是不变，但是需要确保实现代码的高亮（推荐使用
   https://shiki.style/guide/这个库）
5. 当前项目在手机端的 UI 设计存在问题。我希望在手机端的 UI 中，还是主要保留 chat
   的那套逻辑。顶部是顶栏，然后中间主要内容为 messages, 然后底部是 markdown
   输入框。而三栏设计则使用 tab 替代，让用户可以在三个 tab
   之间切换。类似于下面这样，但是其设计可以更加好用更符合人体工程学。

|----------------------------|

| settings save delete theme   |
| ---------------------------- |
|                              |
| message                      |
| message                      |
| message                      |
|                              |
|                              |
| ---------------------------- |
| ---------------------------- |
|                              |
| send                         |
| ---------------------------- |
| room settings                |
| ---------------------------- |

7. 当前的 elizabeth 项目为真正的接入后端 API,
   但是这个可以先不急。先将前端的基本设计跑通之后在着手设计前后端联调的任务。但是在前端的设计过程需要留出对应的接口，方便后续的联调。
